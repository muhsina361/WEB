sql


CREATE DATABASE library;
USE library;

CREATE TABLE books (
    accession_number INT PRIMARY KEY,
    title VARCHAR(255),
    authors VARCHAR(255),
    edition VARCHAR(50),
    publisher VARCHAR(255)
);
